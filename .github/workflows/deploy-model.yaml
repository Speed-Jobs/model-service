name: Build and Push Model Service Image

on:
  push:
    branches:
      - main
    paths:
      - '**'
      - '.github/workflows/deploy-model.yaml'

env:
  IMAGE_NAME: speedjobs-model
  VERSION: "1.0.0"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    environment: model-service

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Harbor
        run: |
          echo "${{ secrets.DOCKER_REGISTRY_PASSWORD }}" | docker login ${{ secrets.DOCKER_REGISTRY }} \
            -u "${{ secrets.DOCKER_REGISTRY_USER }}" \
            --password-stdin

      - name: Build Docker image
        run: |
          docker build --no-cache -t ${{ env.IMAGE_NAME }}:${{ env.VERSION }} .

      - name: Tag and Push Docker image
        run: |
          docker tag ${{ env.IMAGE_NAME }}:${{ env.VERSION }} ${{ secrets.DOCKER_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}
          docker push ${{ secrets.DOCKER_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}

      - name: Setup kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Configure Kubernetes
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBECONFIG }}" | base64 -d > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      - name: Restart Deployment
        run: |
          kubectl rollout restart deployment/speedjobs-model-service -n skala-practice
          kubectl rollout status deployment/speedjobs-model-service -n skala-practice --timeout=5m

      - name: Summary
        run: |
          echo "Docker 이미지 푸시 완료"
          echo "- ${{ secrets.DOCKER_REGISTRY }}/${{ env.IMAGE_NAME }}:${{ env.VERSION }}"
          echo "Kubernetes Deployment 재시작 완료"
