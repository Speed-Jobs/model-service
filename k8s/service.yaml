apiVersion: v1
kind: Service
metadata:
  name: model-service
  namespace: skala-practice
  labels:
    app: model-service
spec:
  selector:
    app: model-service
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP